const input = require("../input");

(async ()=>{
    console.log("Quantos anos você? ");
    let idade = await input();

    if(condição lógica){ //se verdadeiro
        executa o que estive aqui dentro se for verdade
    }else{ // senão
        executa o que estive aqui dentro se for falso
    }

    if()
        //somente uma linha não precisa de chaves
    else
        //somente uma linha não precisa de chaves

    


})();